Purpose: Simulate an IPAM service using a basic API

Usage:
  POST:
    API Path: /ip/request
    When a post request is made the service will generate a random IP address from the subnet 192.168.1.0/24
    {
        "ip address": "IP_ADDRESS"
    }
